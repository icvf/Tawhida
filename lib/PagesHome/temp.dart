import 'package:flutter/material.dart';

class tempc extends StatelessWidget {
  const tempc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "This is tempc Page",
          style: TextStyle(fontSize: 40),
        ),
      ),
    );
  }
}
